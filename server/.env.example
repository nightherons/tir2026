# Database (SQLite - Railway will persist this in a volume)
DATABASE_URL="file:./data/tir2026.db"

# JWT Secret - CHANGE THIS! Generate with: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Server
PORT=3001
NODE_ENV=production

# Frontend URL (for CORS) - Your Vercel/Netlify domain
FRONTEND_URL="https://thenightherons.run"
